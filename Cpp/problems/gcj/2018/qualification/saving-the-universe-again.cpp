// Problem #1 Qualification Round 2018
#include <iostream>
#include <string>

using namespace std;

int main() {
	int t, d;
	char p[30];
	cin >> t; //number of case input

	int output[t];

	//Case Input Loop
	for (int i = 0; i < t; ++i) {
		cin >> n >> m;

		cout << "Case #" << i << ": " << (n + m) << " " << (n * m) << endl;
	}

	//Loop for ouptut
	for (int i = 0; i < t; i++) {
		cout << output[t] << endl;
	return 0;
}
# Goals : To able to compete with every robots registered
import string

def counter_solution(C):
    pass

t = int(input()) #read a line with a single integer
for i in range(1, t + 1):
    A = int(input()) # How many other robots
    C = [] #the program of each robots
    output = ""
    print(type(C))
    for x in range(0,A):
        inp = input() # program input
        C.append(inp)
        
    
    sol = [] #Solution of counter on each scenario.
    
        

    print("Case #{}: {}".format(i, output))